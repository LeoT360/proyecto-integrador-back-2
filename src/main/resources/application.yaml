spring:
  application:
    name: proyecto-integrador-back-2-db
  datasource:
    url: jdbc:mysql://localhost:3306/proyecto-integrador-back-2-db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: admin
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true